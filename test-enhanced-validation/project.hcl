# Spooky Project Configuration
# Generated from Go struct schemas

project {
  name = "test-enhanced-validation"
  description = "Testing enhanced validation rules"
  # run_max_parallel = 10  # Maximum parallel action executions for this project
  # run_dry_run_default = false  # Default dry-run mode for actions
  # run_validate_before_run = true  # Validate project configuration before running
  # run_backup_before_changes = false  # Create backups before making changes
  # facts_timeout = 30  # Timeout for facts collection in seconds
  # facts_parallel_collection = 10  # Number of parallel facts collection workers
  # facts_retry_attempts = 3  # Number of retry attempts for failed facts collection
  # facts_retry_delay = 5  # Delay between retry attempts in seconds
}
