# Test file with multiple inventory blocks
inventory {
  machine "first" {
    host = "localhost"
    user = "user"
  }
}

inventory {
  machine "second" {
    host = "localhost"
    user = "user"
  }
}
