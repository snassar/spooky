# Nginx configuration template for {{hostname}}
# Generated on {{osVersion}} with {{memoryInfo}} RAM

server {
    listen 80;
    server_name {{hostname}} {{ipAddress}};
    
    # Document root
    root /var/www/html;
    index index.html index.php;
    
    # Log files
    access_log /var/log/nginx/{{hostname}}_access.log;
    error_log /var/log/nginx/{{hostname}}_error.log;
    
    # PHP configuration
    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
    
    # Static files
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;
}

# Machine-specific configuration
# Machine ID: {{machineID}}
# Available disk space: {{diskSpace}}
# File exists check: {{if fileExists "/etc/ssl/certs/ssl-cert-snakeoil.pem"}}SSL certificate found{{else}}No SSL certificate{{end}} 