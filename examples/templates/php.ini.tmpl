; PHP Configuration for {{hostname}}
; Generated on {{osVersion}} with {{memoryInfo}} RAM
; Machine ID: {{machineID}}

[PHP]
; Basic settings
memory_limit = 256M
max_execution_time = 300
max_input_time = 300
post_max_size = 64M
upload_max_filesize = 64M

; Error reporting
display_errors = Off
log_errors = On
error_log = /var/log/php/{{hostname}}_error.log

; Session settings
session.save_handler = files
session.save_path = "/var/lib/php/sessions"
session.gc_maxlifetime = 1440

; File uploads
file_uploads = On
upload_tmp_dir = /tmp

; Security settings
expose_php = Off
allow_url_fopen = Off
allow_url_include = Off

; Performance settings
opcache.enable = 1
opcache.memory_consumption = 128
opcache.interned_strings_buffer = 8
opcache.max_accelerated_files = 4000
opcache.revalidate_freq = 2
opcache.fast_shutdown = 1

; Date settings
date.timezone = UTC

; Mail settings
sendmail_path = /usr/sbin/sendmail -t -i

; Custom settings for this server
; Available disk space: {{diskSpace}}
; SSL certificate: {{if fileExists "/etc/ssl/certs/ssl-cert-snakeoil.pem"}}Available{{else}}Not available{{end}} 